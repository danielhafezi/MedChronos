# Database: PostgreSQL connection string
# Replace USER, PASSWORD, HOST, PORT, and DATABASE with your actual database credentials.
# Ensure sslmode=require is used if your database requires SSL.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# Google Cloud Platform
# Path to your Google Cloud service account key JSON file.
# This file should be kept secure and not committed to the repository.
GOOGLE_APPLICATION_CREDENTIALS="./medchronos-key.json"
# Your Google Cloud Project ID.
GCP_PROJECT_ID="your-gcp-project-id"
# The name of your Google Cloud Storage bucket for storing patient study images.
GCS_BUCKET_NAME="your-gcs-bucket-name"

# Vertex AI (Google Cloud AI Platform)
# The Google Cloud region where your Vertex AI resources are located (e.g., us-central1).
VERTEX_AI_LOCATION="your-vertex-ai-location"

# Gemini API
# Your API key for accessing Google's Gemini models.
GEMINI_API_KEY="your-gemini-api-key"

# Application URL (Optional, but recommended for some features like PDF export if absolute URLs are needed)
# The base URL of your deployed application (e.g., http://localhost:3000 for local development)
# NEXT_PUBLIC_APP_URL="http://localhost:3000"
